#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(ql:quickload :project-isidore)

(defun main (&rest argv)
  (declare (ignore argv))
  (project-isidore:initialize-application :productionp nil
                                          :port 8080
                                          :dispatch-folder "assets/")
  (loop (sleep 600)))
