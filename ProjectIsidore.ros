#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(ql:quickload :project-isidore)

(defun main (&rest argv)
  (declare (ignore argv))
  (project-isidore:initialize-application :cmd-user-interface t)
  (loop (sleep 600)))
